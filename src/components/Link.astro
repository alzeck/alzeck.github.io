---
export interface Props {
  href: string;
  label?: string;
}

const { href, label } = Astro.props;
---

<a
  href={href}
  class="text-accent underline decoration-primary underline-offset-4 hover:text-primary"
  >{label ? label : <slot />}</a
>
